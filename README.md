# Sub2API Back4App deployment

Minimal deployment wrapper pinned to Sub2API v0.2.0.

Runtime secrets are configured only in Back4App environment variables; no credentials are stored in this repository.
